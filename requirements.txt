# --- Core ---
# playwright & wasmtime replaced by Node.js helpers (playwright-core + node WebAssembly) on Termux
httpx>=0.27             # async HTTP client for chat streaming
fastapi>=0.99,<0.100    # OpenAI-compatible server
uvicorn>=0.30           # ASGI server
pydantic<2.0            # request/response models (pure-python wheel for Termux compatibility)
python-dotenv>=1.0      # load credentials from .env
